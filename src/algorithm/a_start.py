from typing import List

def a_start():
    opened: List = list()
    closed: List = list()
    success: bool = false
    # while len(opened) and success == True:
    #     selected_node = select_a_node
    #     if selected_node is final:
    #         success = True
    #     else:
    #         opened.remove(selected_node)
    #         closed.append(selected_node)
            # Expand